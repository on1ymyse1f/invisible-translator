# 无感翻译 UI 设计基线

## 产品命题

无感翻译不是聊天客户端，而是一个把“当前文字变成目标语言”的单任务工具。界面的首要问题不是展示更多能力，而是让用户在任何时刻都能回答三个问题：

1. 我正在翻译什么？
2. 会翻译成什么语言？
3. 点击后会发生什么，是否会发送？

因此主翻译窗采用内容优先的工作台，而不是复制聊天软件的完整导航结构。输入区是视觉中心；语言与主题是稳定的次级控制；“翻译并写入”是唯一主操作；“本机翻译 / 只写草稿 / 不会发送”持续说明安全边界。

## 从 Claude 类软件吸收的逻辑

- 低界面噪声：大面积内容画布、少量持续可见控制、明确的输入焦点。
- 稳定的操作位置：目标语言、外观和主按钮不会随状态跳动。
- 阅读优先：温暖中性色代替纯白，深色正文保持足够对比度，状态信息使用第二层级。
- 外观是偏好而不是功能模式：切换主题不会改变翻译来源、写回目标、网络边界或隐私判断。

这些原则用于形成自己的翻译工作台；不复制 Claude 的商标、图标、文案、侧栏或像素级布局。

## 主题系统

### Claude 暖纸（新用户默认）

- 背景：奶油纸张渐变，降低长时间阅读的眩光。
- 强调：陶土红，只用于主按钮、焦点和关键状态。
- 正文：暖黑；卡片接近纸白，边界轻而可辨。
- 氛围：右上角极弱暖色光晕，不与内容竞争。

### Claude 墨夜

- 背景：暖炭黑而非冷蓝黑。
- 强调：柔和橙红，保持与暖纸主题的语义一致。
- 正文：近白；次级文字偏暖灰，避免黑底纯白的视觉疲劳。

### 赛博霓虹

- 背景：近黑青紫渐变。
- 主强调：电光青；辅强调：霓虹品红。
- 装饰：32 pt 低透明网格与两处弱光晕，仅营造空间感。
- 可读性护栏：输入与译文始终位于高不透明卡片；正文不用发光字体；品红不承载长文本；动效仅为 220 ms 主题过渡。

### 跟随系统

保留原生 macOS 材质与系统强调色，适合希望界面与系统设置一致的用户。

## 组件层级

```text
主翻译窗
├─ 身份与安全边界
├─ 目标语言 / 外观
├─ 输入卡片（主焦点）
├─ 回复译文卡片（有内容时出现）
└─ 主操作 / 状态 / 上次译文

选区浮层
├─ 原文与译文
└─ 复制 / 安全替换

AI 兼容边缘栏
├─ 草稿翻译
└─ 回复翻译
```

三类窗口共享同一套颜色令牌，因此主题切换不会造成“主窗是一个产品、浮层是另一个产品”的割裂。

## 可访问性与验收规则

- 不以颜色作为唯一状态信号；关键状态同时有文字或图标。
- 内容卡片与正文保持清晰对比，赛博装饰层不接收点击并对辅助功能隐藏。
- 支持系统主题；固定暖纸、墨夜和赛博主题分别声明明/暗色方案。
- 主题偏好保存到本机；旧版 `tokyoBlue` 偏好自动迁移为“赛博霓虹”。
- UI 合成测试只能证明原生 AppKit/SwiftUI 流程，不能替代真实 ChatGPT、Claude、Electron/WebKit、多显示器和 OCR 运行时验收。
